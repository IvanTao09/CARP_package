from carp import CARPimport numpy as npimport scipy.io as scioif __name__ == '__main__':    path = r'/Users/ivantao/Documents/MATLAB/CARP-master/imagenet_matlab/obs1.mat'    mat = np.array(scio.loadmat(path)['A'])    #print(mat)    sigma_hat = 0.01    dimension = np.array(mat.shape)    obs = np.squeeze(mat.T.reshape(1,mat.size))    hyper = CARP.hyper_3D_default(obs, dimension, sigma_hat, True)    #MAP_0 = np.array(py_tree.get_MAP(obs, dimension, hyper)).astype('int16')    comp_mat, final_mat = CARP.CARP(mat,hyper)